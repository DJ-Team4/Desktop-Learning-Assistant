<Window x:Class="UI.Settings"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:pu="clr-namespace:Panuon.UI.Silver;assembly=Panuon.UI.Silver"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:UI"
        mc:Ignorable="d"
        Title="Settings" Height="400" Width="600">
    <Grid>
        <TabControl 
            HorizontalAlignment="Left" 
            Height="371" 
            VerticalAlignment="Top" 
            Width="593"
            pu:TabControlHelper.ItemHeight="30"
            pu:TabControlHelper.CanRemove="False"
            pu:TabControlHelper.SelectedBackground="AliceBlue"
            TabStripPlacement="Left" 
            Margin="0,0,0,-0.333">
            <TabItem Header="白名单">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="352*"/>
                        <ColumnDefinition Width="137*"/>
                    </Grid.ColumnDefinitions>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="55*"/>
                        <RowDefinition Height="316*"/>
                    </Grid.RowDefinitions>
                    <StackPanel Grid.Row="1" Grid.Column="0" Margin="5,4.8,5,5.2">
                        <ListView x:Name="AllFocusAppView" ItemsSource="{Binding AllFocusApps}" Width="505" Margin="5">
                            <ListView.ItemTemplate>
                                <DataTemplate>
                                    <WrapPanel Width="300">
                                        <StackPanel  Orientation="Vertical">
                                            <WrapPanel>
                                                <!--TextBlock Text="{Binding AppName}" FontSize="18" Height="28" /-->
                                                <CheckBox Content="{Binding AppName}" IsChecked="{Binding isFocus}" Height="40"/>
                                            </WrapPanel>
                                        </StackPanel>
                                    </WrapPanel>
                                </DataTemplate>
                            </ListView.ItemTemplate>
                        </ListView>
                    </StackPanel>
                    <Label Content="选择默认设置的白名单以统计任务效率" HorizontalAlignment="Left" Margin="10,10,0,0" VerticalAlignment="Top" Height="33" Width="251"/>
                    <Button Content="增加" HorizontalAlignment="Left" Margin="31,89.8,0,0" VerticalAlignment="Top" Width="75" RenderTransformOrigin="2.884,6.097" Grid.Row="1" Grid.Column="1" Height="19"/>
                    <Button Content="删除" HorizontalAlignment="Left" Margin="31,130.8,0,0" VerticalAlignment="Top" Width="75" RenderTransformOrigin="2.884,6.097" Grid.Row="1" Grid.Column="1" Height="19"/>
                </Grid>
            </TabItem>
            <TabItem Header="软件类型设置">
                <Grid>
                    <Border BorderBrush="Black" BorderThickness="1" HorizontalAlignment="Left" Height="284" Margin="10,39,0,0" VerticalAlignment="Top" Width="454">
                        <Grid>
                            <Label Content="软件:" Width="50" Margin="10,19,392.8,234.4"/>
                            <Label Content="类型:" Width="50" Margin="214,19,188.8,234.4"/>
                            <ComboBox Name="SoftWareComboBox" Height="32" Margin="60,16,265.8,234.4" ItemsSource="{Binding Path=SoftWareList}" SelectionChanged="SoftWareSelectionChanged"/>
                            <ComboBox Name="TypeComboBox" Height="32" Margin="264,16,61.8,234.4" ItemsSource="{Binding Path=TypeList}" SelectionChanged="TypeSelectionChange"/>
                        </Grid>
                    </Border>
                    <Button Content="确定" HorizontalAlignment="Left" Margin="389,328,0,0" VerticalAlignment="Top" Width="75"/>
                    <Label Content="设置各软件的软件类型" Margin="168,10,169.333,332.667"/>
                </Grid>
            </TabItem>
            <Border BorderBrush="Black" BorderThickness="1" Height="178" Width="254">
                <Border BorderBrush="Black" BorderThickness="1" HorizontalAlignment="Left" Height="32" Margin="0,0,-60,-32" VerticalAlignment="Top" Width="60"/>
            </Border>
        </TabControl>
    </Grid>
</Window>
